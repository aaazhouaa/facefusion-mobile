<?xml version="1.0" encoding="utf-8"?>
<!--
    Window-chrome colours only.

    Compose does not read this file for its colour scheme — that lives in ui/Theme.kt.
    What is here exists so the window behind Compose is already the right colour before
    the first frame is composed, otherwise launching flashes.

    This file is the LIGHT value and values-night/ carries the dark one; the pair has to
    track ui/Theme.kt's two schemes, which follow the phone.
-->
<resources>
    <color name="ff_background">#FFF7F8FA</color>

</resources>
