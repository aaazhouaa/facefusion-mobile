<!--
  Material's `playlist_add`: a list with a plus, for "add to the batch queue".

  Declared here rather than depended on, same as ic_person_off: material-icons-core
  carries a fixed short list and this is not on it; the rest live in
  material-icons-extended, thousands of vectors for one. Google's own path data,
  unchanged, on the standard 24 dp viewport.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M14,10H2v2h12v-2zM14,6H2v2h12V6zM18,14v-4h-2v4h-4v2h4v4h2v-4h4v-2H18zM2,16h8v-2H2v2z" />
</vector>
